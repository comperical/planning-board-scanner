PLANSCAN TODO
=============

- [DONE 2026-09-15] .docx text-extraction support was added to
  wwiocode/pyscript/plan_util.py (python-docx, installed in the KitchenSink
  venv) - PdfInfoTool/PdfKeywordScanTool/PdfExtractTextTool/IngestPdfTool
  all accept pdf=working/<path>.docx now, treating the whole document as a
  single page (docx has no real page boundaries). PdfRenderPagesTool
  remains PDF-only (no page-image equivalent for docx). Ingested the 3
  known .docx cases: Hampton Falls, NH (2026-08-25 PB Agenda.docx and
  _06232026-147, renamed to _06232026-147.docx) and Gilford, NH
  (PB_Notice_for_August_17_2026_jba.docx). Brentwood, NH's docx files are
  still blocked - see the hotlink item below, that town also needs the
  base64 workaround before its files even reach disk.

- [DONE 2026-09-21] "Hotlink-protected" downloads. Two different causes,
  neither of which was actually hotlink protection:
  * Concord (Legistar View.ashx): the 410 Gone came from ArgMap splitting
    on every "=", truncating target=...View.ashx?M=A&ID=... to
    ...View.ashx?M. plan_entry.py now splits args on the first "=" only;
    plain FetchUrl works.
  * Kingston, Madbury, Brentwood: Cloudflare "Just a moment..." challenge
    on EVERY non-browser request (listing pages too - Referer/headers don't
    help), and on in-page fetch() too. What works: a native
    <a download> click in the headed planscan session (browser saves to
    working/playwright_output/), then the new ClaimDownload tool checks the
    PDF/.docx signature and moves it into working/<town>/. No base64 step
    needed. FetchUrl now also refuses to save non-PDF/.docx responses.
  Upstream: /opt/userdata/crm/src/python/opsutil/ArgMap.py getFromArgv
  still has the split("=") bug for other callers.

- [DONE 2026-09-21] scan_log table - exists in plan_db.py with
  StartScanLog/EndScanLog, documents.first_scan_id, and NextToScan /
  towns.wisp "Last Scanned" built on it.
